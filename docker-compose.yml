version: '3.1'
services:
  build-images:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./opt:/opt
      - ./images:/images
      - ./.ccache:/root/.cache/ccache
    command: "${SS_ARGS:---no-op}"