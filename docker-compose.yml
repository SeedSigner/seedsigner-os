version: '3.1'
services:
  build-images:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./opt:/opt
      - ./images:/images
    command: "${SS_ARGS:---no-op}"